package week1.shapedemo;

import java.util.ArrayList;

/**
 * Driver class to test that the Circle and Rectangle classes
 * properly implement the Shape interface.
 * */


public class Driver {
    /*public static double sumAreas(ArrayList<Shape> shapes) {
        double totalArea = 0.0;
        for(int i = 0; i < shapes.size(); i++) {
            Shape shape = shapes.get(i);
            totalArea += shape.area();
        }

        return totalArea;
    }

    public static void main(String[] args) {

        Circle biggerCircle = new Circle("red", 5.0, 0.0, 0.0);
        Circle smallerCircle = new Circle("green", 2.0, 0.0, 0.0);
        Rectangle square = new Rectangle("blue", 4.0, 4.0, 0.0, 0.0);
        Rectangle rectangle = new Rectangle("yellow", 2.0, 8.0, 0.0, 0.0);

        // test maximum areas
        if(Shape.maximumArea(biggerCircle, smallerCircle) == 1) {
            System.out.println("Correct! Bigger circle is bigger!");
        } else {
            System.out.println("Check your calculations!");
        }

        if(Shape.maximumArea(square, rectangle) == 0) {
            System.out.println("Correct!  They have equal areas!");
        } else {
            System.out.println("Check your calculations!");
        }

        // test array list
        ArrayList<Shape> shapes = new ArrayList<>();
        shapes.add(biggerCircle);
        shapes.add(smallerCircle);
        shapes.add(square);
        shapes.add(rectangle);

        double totalArea = sumAreas(shapes);
        System.out.println("The total area of the shapes is: " + totalArea);
    }*/
}
